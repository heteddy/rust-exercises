/*!
 * 实现双向链表，双向列表需要修改已经move的或者借用的node，因此需要使用refcell
*/
#![cfg_attr(
    debug_assertions,
    allow(
        unused,
        dead_code,
        unused_imports,
        unused_variables,
        unused_assignments,
        non_snake_case
    )
)]
mod stack2;
// mod stack2;

fn main() {
    println!("Hello, world!");
}
