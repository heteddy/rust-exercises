#![cfg_attr(
    debug_assertions,
    allow(
        unused,
        dead_code,
        unused_imports,
        unused_variables,
        unused_assignments,
        non_snake_case
    )
)]

use std::io::Write;
use std::process::{Command, Stdio};

mod tests {
    #[test]
    fn test_process() {}
}
